/**
 * Enumeration representing the status of a General Ledger transaction (also known as a Journal Entry)
 */
enum TransactionStatus {
    Pending = 'Pending',
    Posted = 'Posted',
}

export default TransactionStatus;
